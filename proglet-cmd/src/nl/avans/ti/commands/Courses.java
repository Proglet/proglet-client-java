package nl.avans.ti.commands;

import nl.avans.ti.util.Command;

@Command(command = "courses", description = "Shows and register for courses on the server")
public class Courses implements Runnable {
    @Override
    public void run() {

    }
}
